include <stdio.h>

int main (){

 int time, h, m, s;
 printf("Escreva um tempo em segundos: \n");
 scanf("%d", &time);

   h = time / 3600;
   m = (time % 3600) / 60;
   s = (time % 3600) % 60;
{
  printf("\n");
  printf("O tempo total foi de %d:%d:%d", h, m, s);

}
return 0;
}
